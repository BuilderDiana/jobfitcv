
##
前端:
next api route 发送api给后端fastapi

后端:

数据库:
在aws的RDS上新建了数据库

## to-to
1.新建EC2在同一个VPC去链接数据库
好，已记录进度 👍
明天我们从 EC2 开始，一步一步来，不复杂：
明天的起点计划（很短）：
用同一个 VPC创建一台最小的 EC2（t3.micro）
讲清楚：什么是 EC2、什么是 VPC、它们怎么配合 RDS
在 EC2 里装 psql
用 私有网络 连上 RDS（不走公网）
你已经把最难的部分（RDS）搞定了，剩下的是“连线”。
明天直接继续，不用回顾。